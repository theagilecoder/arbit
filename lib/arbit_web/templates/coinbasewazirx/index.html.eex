<h1>Coinbase - WazirX</h1>

<h3>Coinbase - WazirX Bids</h3>
<table>
  <thead>
    <tr>
      <th>Coin</th>
      <th>Coinbase Quote</th>
      <th>Coinbase Price</th>
      <th>WazirX Bid Price</th>
      <th>Difference</th>
      <th>WazirX Volume</th>
    </tr>
  </thead>

  <tbody>
    <%= for result <- sort_by_bid_difference(@results) do %>
      <tr>
        <td><%= result.coin %></td>
        <td><%= result.coinbase_quote %></td>
        <td><%= result.coinbase_price |> format_dollar() %></td>
        <td><%= result.wazirx_bid_price |> format_rupee() %></td>
        <td><%= result.bid_difference |> format_percentage %></td>
        <td><%= result.wazirx_volume |> format_coin_units() %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<h3>Coinbase - WazirX Asks</h3>
<table>
  <thead>
    <tr>
      <th>Coin</th>
      <th>Coinbase Quote</th>
      <th>Coinbase Price</th>
      <th>WazirX Ask Price</th>
      <th>Difference</th>
      <th>WazirX Volume</th>
    </tr>
  </thead>

  <tbody>
    <%= for result <- sort_by_ask_difference(@results) do %>
      <tr>
        <td><%= result.coin %></td>
        <td><%= result.coinbase_quote %></td>
        <td><%= result.coinbase_price |> format_dollar() %></td>
        <td><%= result.wazirx_ask_price |> format_rupee() %></td>
        <td><%= result.ask_difference |> format_percentage %></td>
        <td><%= result.wazirx_volume |> format_coin_units() %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<ul>
  <li>* Last updated at IST Time <%= ist_time(@results) %></li>
  <li>* Percentages are calculated over the 1st exchange</li>
</ul>
